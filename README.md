#grahamg.dev

Personal website built with Jekyll and hosted on personal server.

To build locally:
Run `make build` to build the site and `make serve` to serve the site locally.

To clean the build directory:
Run `make clean`.
